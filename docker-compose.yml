services:
  dev:
    build: .
    container_name: procedure_adventures_dev
    volumes:
      - .:/app
      - cargo-cache:/usr/local/cargo/registry
    working_dir: /app
    stdin_open: true
    tty: true
    environment:
      - TERM=xterm-256color

volumes:
  cargo-cache: